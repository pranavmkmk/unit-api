pipeline {
    agent any
    stages { 
        stage('Example') {
            steps {
                echo 'Hello World'
                script {
                    println "printtag = ${tag_name}"
                }
            }
        }
    }
}
